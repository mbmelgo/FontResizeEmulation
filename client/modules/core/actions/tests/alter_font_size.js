const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../alter_font_size';

describe('core.actions.alter_font_size', () => {
  it('should do something');
});
